# The backend server address to connect to
host: "http://localhost:8080/"

#The contrast mode of the app
contrastMode: "false"

# The current locale
current-locale: "English"

# List of supported locales
supported-locales:
  - name: "English"
    code: "en"
  - name: "Nederlands"
    code: "nl"

# The invite codes of the past events
past-codes: ["b18291d8"]